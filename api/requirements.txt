# Vercel-Optimized Requirements - MINIMAL for 250 MB limit
# This file is specifically for the /api serverless function
#
# After editing, regenerate the lock file:
#   pip-compile --output-file=api/constraints.txt --strip-extras --no-header --allow-unsafe api/requirements.txt
# Verify: bash scripts/check_constraints.sh

# Core FastAPI (Essential)
fastapi==0.109.0
python-multipart==0.0.9

# Authentication & Security (Essential)
PyJWT==2.8.0

# Supabase Client (Essential)
# Pinned to avoid pip backtracking — tested compatible with pydantic 2.5.x
supabase==2.16.0

# OpenAI (Essential for AI features)
# v1.70.0+ required for GPT-5 parameters (reasoning_effort, max_completion_tokens)
openai>=1.70.0
requests>=2.31.0

# Google Cloud Vision OCR (primary OCR engine, ~20 MB with deps)
google-cloud-vision>=3.7.0,<4.0

# Configuration (Essential)
python-dotenv==1.0.1
pydantic==2.5.3
pydantic-settings==2.1.0

# Rate Limiting (Essential)
slowapi==0.1.9

# SSE for streaming (Essential)
sse-starlette==2.0.0

# Lightweight utilities (Essential)
tenacity==8.2.3
PyYAML==6.0.1

# Document processing - Lightweight alternatives
# PyMuPDF - 60 MB (removed, too large)
# PyPDF2 - 10 MB (removed, replaced with pypdf)

# Lightweight PDF extraction (pure Python, ~2MB)
pypdf>=4.0.0

# DOCX extraction (required — DOCX uploads are a supported document type)
python-docx>=1.1.0,<2.0

# Image compression for document processing (re-enabled on Pro plan)
Pillow>=10.0.0

# Lightweight text processing
html2text==2020.1.16
markdown2>=2.4.0

# HTTP client for Cloud Run OCR service
httpx>=0.27.0
