# Unified Requirements for Legal Document Analysis Portal
# Vercel-optimized: Only essential dependencies for serverless deployment

# Authentication and Security
PyJWT==2.8.0
bcrypt==4.1.2

# API Integration
requests>=2.31.0
openai>=1.70.0
tiktoken>=0.5.1
google-cloud-vision>=3.0.0

# Configuration and Data Management
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
PyYAML>=6.0

# Document Processing (REQUIRED for Clio import and document upload)
python-docx>=1.1.0
PyMuPDF>=1.23.0
Pillow>=10.0.0
pillow-heif>=0.16.0
PyPDF2>=3.0.0

# Document Output (REQUIRED for analysis results)
html2text>=2020.1.16
markdown2>=2.4.0

# Utility Libraries
tenacity>=8.2.3

# FastAPI Backend
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.6
supabase>=2.0.0
slowapi>=0.1.9

# SSE (Server-Sent Events) support for real-time progress
sse-starlette==2.1.3

# Redis (optional but required for multi-instance progress tracking)
redis>=5.0.0

